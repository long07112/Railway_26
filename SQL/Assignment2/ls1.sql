CREATE DATABASE Sale_management;
USE Sale_management;

CREATE TABLE Customers (
customers_id 			INT,
first_name 				VARCHAR(50),
date_of_purchare	 	DATE
);

CREATE TABLE sales (
customers_id 			INT,
first_name 				VARCHAR(50),
date_of_purchase 		DATE
);
